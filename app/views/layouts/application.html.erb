<!DOCTYPE html>
<html>
<head>
    <% # Each page has its own title %>
    <title><%= yield(:title) %></title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <% # Display this if user is logged in %>
    <% if is_logged_in? %>
        <div class="alert alert-info" role="alert">
            Logged in as <%= current_user.email %><%= link_to "Log out", logout_path, class: "nav-link-right" %><%= link_to "Dashboard", current_user, class: "nav-link-right" %> 
        </div>
        
    <% end %>
    <% # Each page has its own header %>
    <div class="page-header">
        <h1 class="text-center"><%= yield(:header) %></h1>
    </div>
    <%= yield %>
    <% # Display debug information on development enviroment %>
    <%= debug(params) if Rails.env.development? %>
  </div>
</body>
</html>
